{
  "name": "nearby-web",
  "private": true,
  "scripts": { "dev": "wrangler dev", "deploy": "wrangler deploy", "check": "tsc --noEmit", "test": "node test/pair.mjs" },
  "devDependencies": { "wrangler": "^4", "@cloudflare/workers-types": "^5", "typescript": "^5" }
}
